# Pick one. Default is 'anthropic'. Override per-run with `--provider openai`.
ORCHESTRATOR_PROVIDER=anthropic

# Required
ANTHROPIC_API_KEY=
OPENAI_API_KEY=



# optional tuning
ORCHESTRATOR_MAX_ITERATIONS=30
ORCHESTRATOR_AUTO_APPLY_THRESHOLD=0.85